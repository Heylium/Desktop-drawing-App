
import gesture.MotionEvent


@Composable
fun MyCanvase() {

    Canvas(
        modifier = Modifier
            .fillMaxSize()
            .background(color = Color.White)
    ) {

    }
}